{
  "name": "@yaowu/meeting-export-gateway",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["client", "packages/*"],
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "preflight": "bun scripts/preflight.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "mysql2": "^3.23.1",
    "@yaowu/mde-engine": "workspace:*"
  }
}
