# 绝不能打进镜像的凭证文件
.env
.env.*
!.env.example

.git
.gitignore
.superpowers
.claude
.playwright-mcp

# 任意层级的依赖与构建产物都在容器里重新生成（console/node_modules 里有 playwright，
# 光它一个就是几百 MB；pattern 不带 **/ 只匹配根目录那一个）
**/node_modules
**/dist
**/build

**/tests
docs

*.md
*.log
*.sqlite
*.sqlite-wal
*.sqlite-shm
.DS_Store
